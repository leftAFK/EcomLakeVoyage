# ============================================================
# EcomLakeVoyage - Python 依赖清单
# ------------------------------------------------------------
# 仅本地模拟数据生成器与日志模拟器需要这些库
# Flink SQL ETL 作业在 Docker 容器内运行, 无需本地安装
# ------------------------------------------------------------
# 安装:  pip install -r requirements.txt
# 升级:  pip install --upgrade -r requirements.txt
# 国内加速: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================================

# ---------- MySQL 写入 ----------
pymysql>=1.1.0              # MySQL 协议驱动, 数据生成器写库

# ---------- 高逼真模拟数据 ----------
Faker>=20.0.0              # 多语言假数据 (中文名/手机号/地址/邮箱...)

# ---------- 概率分布 (可选, 未安装则降级为 random) ----------
numpy>=1.24.0              # beta/gamma/normal 分布, 让价格/年龄/库存更真实

# ---------- Kafka 日志推送 ----------
kafka-python>=2.0.2        # Kafka 生产者, 发送埋点日志到 topic
